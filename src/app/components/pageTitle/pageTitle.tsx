
const PageTitle = ({content}: {content: string}) => {
    return (
        <h1 className="mt-4 text-xl font-bold underline text-gray-900 text-center">{content}</h1>
    );
}

export default PageTitle;
        